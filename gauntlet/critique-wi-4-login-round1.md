# WI-4 login — round 1
Verdict: PASS

## Criteria
- CORRECTNESS: Met. `parse_pairing_url` mirrors t3code `packages/shared/src/remote.ts` `resolveRemotePairingTarget({ pairingUrl })`: hash `#token=` wins over query `?token=`; hosted `?host=` plus token uses `_normalize_remote_base_url` (strip leading slashes, prepend `https://` when no scheme, keep `http/https/ws/wss`, collapse `//https://…`); `ws`/`wss` become `http`/`https`; origin path is `/`. Independent probe matched every `remote.test.ts` pairingUrl case plus hosted-host normalizations (`//host`, `//host:3000`, `///example.com`, `//https://example.com`, `myserver.com:3000`) and the real `t3 pair` LAN shape `http://192.168.1.42:3773/pair#token=PAIRCODE`. `login()` fetches the unauthenticated descriptor, `exchange_pairing`s the hash/query credential (not the access token), stores `access_token` via `set_secret(secret_name_for(name), …)`, upserts `environments.<name> = {base_url}`, and names the env from `slugify_label(descriptor.label)` (`My Laptop` → `my-laptop` → `T3CODE_TOKEN_MY_LAPTOP`); `--name` wins; empty slug falls back to `environmentId`. CLI prints name, base_url, scope, `expires_in` — not the bearer. 401 surfaces the WI-2 re-pair hint. `logout` deletes the secret and drops the config row.
- CONTRACT: Met. `import talaria.auth_direct` / `talaria.cli` and `register(ctx)` open no sockets (`socket_guard` plus a live constructor-deny probe: 8 tools, CLI `t3code`). `register_cli` still wires `setup_fn` + `handler_fn` that closes over `ctx` (hermes `set_defaults(func=handler_fn)`). Missing `hermes t3code login` URL still returns 0 (WI-1 `test_register_t3code_cli` and `test_cli_missing_pairing_url_returns_zero`); same for missing logout env; connect/environments remain stub 0. No hermes import at module load (`save_env_value` / `remove_env_value` lazy inside set/remove). Unknown descriptor/exchange fields are not stripped (NFR-5); login only reads `label` / `environmentId` / `access_token` / `scope` / `expires_in`.
- SECURITY: Met. Bearer goes through `config.set_secret` (test `store=`, else `hermes_cli.config.save_env_value` → `~/.hermes/.env`). `_upsert_environment` writes only `base_url`. Pairing credential is the RFC 8693 `subject_token`, never persisted. `_print_login` and CLI stderr on 401 do not echo `tok-test` / `tok-pair`. Tests use those short fakes only. NFR-2 holds for this item.
- TESTS: Met. `tests/test_auth_direct.py` drives `httpx.MockTransport` (no live server, no `sleep`). AC1: `login()` on local `#token` and hosted `?host=` + `#token` both exchange and store; hosted requests hit `desktop.tailnet.ts.net:44342`, not `app.t3.codes`. AC2: `_config_blob` asserts `ACCESS`/`PAIR` absent from `ctx` settings and `set_config` writes; secret map holds `T3CODE_TOKEN_MY_LAPTOP=tok-test`. Also: legacy query token, `--name`, unknown fields, exchange 401 stores nothing, logout, CLI scope/expiry print without token, missing-url returns 0.
- SIMPLICITY: Met. Stdlib `urlparse`/`parse_qsl` plus the small `remote.ts` helpers — not a parser framework. One `login`/`logout` pair, google_meet-style argparse tree, `T3EnvClient` reused (no second HTTP stack). Same weight as hermes `plugins/spotify/client.py` plus a CLI. `store=`/`client=` are test seams on the command function, not a strategy hierarchy.

## Defects

## Probes run
- `python3 -m pytest tests/test_auth_direct.py tests/test_scaffold.py tests/test_config.py tests/test_t3_env.py -q` → 72 passed in 0.20s. Concurrent dirty `talaria/tools.py` / `talaria/schemas.py` / `tests/fixtures/` / `tests/test_read_tools.py` ignored.
- `git status --short`: WI-4 surface is `M talaria/cli.py`, untracked `talaria/auth_direct.py`, `tests/test_auth_direct.py`.
- Pairing parse probe vs `remote.test.ts` pairingUrl cases: all four match (`#token`, `?token`, hosted encoded `host=https://desktop…:44342/`, protocol-relative `%2F%2Fremote.example.com`). Hosted-host extras from `normalizeRemoteBaseUrl` also match. Rejects: ftp pairing URL, ftp hosted backend, missing token, `not a url`. LAN `t3 pair` URL → `http://192.168.1.42:3773/`.
- Login probe (MockTransport): hash origin `remote.example.com`, hosted origin `desktop.tailnet.ts.net:44342`; `store={'T3CODE_TOKEN_MY_LAPTOP': 'tok-test'}`; config `{my-laptop: {base_url: …}}` contains neither `tok-test` nor `tok-pair`; form `subject_token=tok-pair`. CLI stdout is name/base_url/scope/expires_in only. 401 CLI: exit 1, hint `run \`hermes t3code login <pairing-url>\` to pair this environment`, empty store/config, pairing token not printed (401 body discarded by WI-3).
- Socket-deny: wipe `talaria*`, patch `socket.socket` / `create_connection`, `import talaria` + `register(Fake)` → 8 tools, CLI `t3code`. Registered handler: `t3code login` / `logout` with no args → 0.
- Source: no `sleep` / `requests` / `urllib.request` in the three WI-4 files; no lark/pyparsing/etc.; `set_secret` + `set_config(..., {name: {base_url}})` only.
