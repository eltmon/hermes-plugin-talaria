# WI-8 clerk-oauth — round 1
Verdict: PASS

## Criteria
- CORRECTNESS: Met. `clerk_frontend_api_hostname` mirrors t3code `packages/shared/src/relayAuth.ts` `parseClerkFrontendApi`: strip `pk_live_`/`pk_test_`, standard-base64 decode, drop trailing `$`, take URL hostname. Independent probe: `pk_live_Y2xlcmsudDMuY29kZXMk` decodes to `clerk.t3.codes$` → `clerk.t3.codes`; token URL is `https://clerk.t3.codes/oauth/token` (`publicConfig.ts` `${clerkFrontendApiUrl}/oauth/token`). PKCE matches `CliTokenManager.makePkceRequest`: 32-byte verifier, S256 `base64url(SHA256(ascii(verifier)))` unpadded, 16-byte state (43/43/22). Authorize URL is `{hosted}/connect#state=…&challenge=…` with no query and no `port` (`buildConnectAuthorizeRequestUrl` out-of-band). Probe against `connectAuth.test.ts` literals produced `https://app.t3.codes/connect#state=q7mK9xV2pL4nR8sT6wYzAQ&challenge=E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM`. Blob parse is `lastIndexOf(".")` (dots inside the code kept); malformed / wrong-state return the exact `checkConnectAuthCode` strings and never POST. Token exchange body is form-urlencoded `grant_type=authorization_code`, `code`, `redirect_uri=https://app.t3.codes/connect/callback`, `client_id=hzxSgY2cH10sDU2r`, `code_verifier` — the `outOfBandOAuthLogin` / `exchangeToken` shape. Refresh is `grant_type=refresh_token` + `refresh_token` + `client_id`; omitted refresh_token keeps the old one (`response.refresh_token ?? params.refresh_token`). Public identifiers match the PRD defaults.
- CONTRACT: Met. `import talaria.auth_t3connect` / `talaria.register(FakeCtx)` open no sockets (`socket_guard` plus a live constructor-deny after httpx/ssl are loaded). `register_cli` still wires `setup_fn` + `handler_fn` closing over `ctx`. Missing `login` URL / `logout` env still return 0 (WI-1 `test_register_t3code_cli` and WI-4 CLI tests). `hermes t3code connect` with no `--code` prints the authorize URL and returns 0; `--code` completes or prints the user-facing error on stderr (exit 1). No hermes import at module load (`save_env_value` / `remove_env_value` lazy inside pending save/clear). `get_client()` stays a lazy singleton and is not called at import.
- SECURITY: Met. Clerk access/refresh go through `config.set_secret` as `T3CODE_CLERK_ACCESS_TOKEN` / `T3CODE_CLERK_REFRESH_TOKEN` (test `store=`, else `hermes_cli.config.save_env_value`). `complete_connect` does not call `ctx.set_config`; config blob probes contain neither fake tokens nor the PKCE verifier. Short-lived PKCE session (verifier/state) is stored as `T3CODE_CLERK_PKCE_PENDING` in the secret store, not config, and is removed after a successful exchange. CLI prints the authorize URL (challenge is public) and `Connected to T3 Connect` — not the tokens. Tests use `tok-clerk-access` / `tok-clerk-refresh` only. NFR-2 holds for this item.
- TESTS: Met. `tests/test_clerk_oauth.py` drives `httpx.MockTransport` (no live Clerk, no `sleep`). AC1: `test_publishable_key_derives_clerk_t3_codes`. AC2: unit + CLI tests for malformed (`That does not look like a T3 Connect code…`) and wrong state (`That code belongs to a different connect request…`), with no HTTP. Also: S256 challenge, unique verifier/state, hash authorize URL without `port`, config overrides (hosted/client/pk from `CliTokenManager.test.ts`), token-exchange field set, secrets-not-config, refresh shape + omitted refresh_token, CLI two-step without leaking tokens. 22 passed.
- SIMPLICITY: Met. Stdlib `hashlib`/`secrets`/`urlencode` plus the small `connectAuth.ts` / `relayAuth.ts` helpers — not an OAuth framework. One `start_connect`/`complete_connect`/`refresh_clerk_tokens` surface, google_meet-style argparse, shared `get_client()` (no second HTTP stack). Two-step `--code` matches hermes `plugins/platforms/google_chat/oauth.py` (`--auth-url` then exchange) and keeps `connect` returning 0 for the WI-1 CLI probe. `store=`/`client=` are test seams on the command function. Line count 389 vs hermes `plugins/spotify/client.py` 435.

## Defects

## Probes run
- `python3 -m pytest tests/test_clerk_oauth.py -q` → 22 passed in 0.10s. `tests/test_auth_direct.py tests/test_scaffold.py` → 30 passed in 0.14s. Concurrent dirty `talaria/tools.py` / `talaria/schemas.py` / `talaria/commands.py` / `tests/test_write_tools.py` ignored.
- `git status --short`: WI-8 surface is `M talaria/cli.py`, untracked `talaria/auth_t3connect.py`, `tests/test_clerk_oauth.py`.
- pk / URL / blob probe vs upstream: `pk_live_Y2xlcmsudDMuY29kZXMk` → `clerk.t3.codes`; token endpoint `/oauth/token`; authorize URL matches `connectAuth.test.ts` hash pair and has no `port`; `parse_connect_auth_code("az9.code.chunk.state-uuid")` keeps dots in the code; malformed/wrong-state strings equal `connectAuth.ts` `checkConnectAuthCode`. PKCE 43/43/22 unpadded S256.
- Token-exchange probe (MockTransport, in-memory `_pending` cleared to force secret-store reload): POST `https://clerk.t3.codes/oauth/token` form is exactly `{grant_type, code, redirect_uri=https://app.t3.codes/connect/callback, client_id=hzxSgY2cH10sDU2r, code_verifier}`; store has the two Clerk secrets; config writes empty; pending secret cleared. Refresh form is `{grant_type=refresh_token, refresh_token, client_id}`. Wrong-state/malformed raise before HTTP; store has no access token.
- Socket-deny: wipe `talaria*`, patch `socket.socket` / `create_connection` after `httpx` is loaded, `import talaria` + `register(Fake)` → 8 tools, CLI `t3code`. Handler: `login` / `logout` missing args → 0; `connect` prints `https://app.t3.codes/connect#state=…&challenge=…` (no `port=`) and returns 0.
- Source: no `sleep` / `requests` / `urllib.request` in the three WI-8 files; `set_secret` only for Clerk tokens + pending PKCE JSON; FakeCtx `set_config` is never called from `auth_t3connect.py` / `cli.py`. `hermes_cli` is not importable here; `~/.hermes/.env` has no `T3CODE_CLERK_*` keys after the suite.
