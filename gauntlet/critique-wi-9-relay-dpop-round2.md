# WI-9 relay-dpop — round 2
Verdict: PASS

## Criteria
- CORRECTNESS: Met. AC1/AC2/AC3 and the four round-1 defects all hold on a fresh probe, not just the builder’s tests. ES256 compact proofs verify (`typ=dpop+jwt`, `alg=ES256`, `jwk` without `d`, `htm`/`htu`/`iat`/`jti`; `ath` = unpadded base64url(SHA-256(token)) only when an access token is present); RFC 7638 thumbprint is SHA-256 of canonical `{crv,kty,x,y}`; `htu` strips query and fragment. `POST /v1/client/dpop-token` form matches t3code `RelayDpopAccessTokenRequest` / `managedRelay.ts` `exchangeAccessToken` (dpop header + Clerk `subject_token`, no `Authorization` bearer): `grant_type` token-exchange, `subject_token_type=urn:ietf:params:oauth:token-type:jwt`, `resource` = relay origin, `scope=environment:connect environment:status`, `client_id=t3-web`. Status/connect send `Authorization: DPoP` + proof with `ath`; connect body is `{clientKeyThumbprint}` (deprecated alias still sent by `managedRelay.ts` and accepted as `clientKeyThumbprint ?? clientProofKeyThumbprint`). Environment `/oauth/token` uses bootstrap `subject_token_type` plus a dpop header and no `ath`. Injected `now=`: first `ensure_environment_access` is connect+exchange; `now+50` is a cache hit; access inside the 5-minute skew with a live credential re-exchanges `/oauth/token` only; `now+(3600-300)+1` with `cred_ttl=100` does a second connect and a second `/oauth/token`. `_ensure_discovered` in [`talaria/tools.py:140`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/tools.py:140) re-raises `NotAuthenticated` / `T3ApiError` / `ConnectAuthError`; Mode-B-only Clerk 401 is `{"error":"not authenticated","hint":"run \`hermes t3code connect\` to sign in to T3 Connect"}`, not “no environments configured”. `_clerk_access` raises `RelayNotAuthenticated` (connect hint). `_probe_env` for `mode=="t3connect"` with a Clerk token calls `POST /v1/environments/:id/status` and reads `status=="online"` plus optional `descriptor` for `label`/`environmentId`; mode A still uses the unauthenticated descriptor. Independent merge path list is list + mode-A descriptor + dpop-token + two `/status` POSTs, zero tunnel `/.well-known`. `discovered_environment_map` falls back to `environmentId` when the slug collides with a mode-A name or another discovered row; `resolve_environment(ctx, "env-laptop-relay")` succeeds and mode-A `laptop` stays `direct`. `sync_discovered_environments` retries list once after `refresh_clerk_tokens` when a refresh secret is stored. FR-1/FR-9/FR-12 hold.
- CONTRACT: Met. `import talaria` / `talaria.dpop` / `talaria.auth_t3connect` / `talaria.tools` plus `register(FakeCtx)` open no sockets (`socket_guard` and a live constructor-deny after httpx/ssl are loaded). `get_client()` stays lazy; cryptography is imported only inside key/sign functions, not at module import (`talaria/dpop.py` top-level imports are stdlib + `config`). Handlers still `(args, **kwargs) -> str` JSON and never raise: `TalariaError.to_json()` plus `_unexpected`. Probe: Mode-B `t3_list` 401, junk `args` (`environment=1`), extra kwargs all return JSON. Missing ctx (or scaffold Fake without `get_config` for wait/write) still hits the WI-1 stub path; `register()` binds ctx so a subsequent no-kwargs call uses that ctx.
- SECURITY: Met for NFR-2 / Decision 7. `T3CODE_DPOP_KEY` is base64(PKCS8 PEM) via `set_secret`, never raw PEM, never `ctx.set_config`. Clerk access stays `T3CODE_CLERK_ACCESS_TOKEN`. Probe: config blob and `config_writes` contain neither the Clerk fake nor the DPoP secret; tool JSON for 401 drops the body (`LEAK-ME` / `tok-clerk-access-LEAK` / `secret-body-xyz` absent). Tests use `tok-clerk-access` / generated in-memory keys only. Fixtures have no PEM. Public `pk_live_…` and `DEFAULT_RELAY_URL` are identifiers, not secrets.
- TESTS: Met. [`tests/test_dpop.py`](/home/eltmon/Projects/hermes-plugin-talaria/tests/test_dpop.py) + [`tests/test_relay.py`](/home/eltmon/Projects/hermes-plugin-talaria/tests/test_relay.py) are MockTransport / injected `now=` / no `time.sleep` / no live relay. AC1 proofs, AC2 clock refresh, AC3 merge (online + offline `/status`, collision keyed as `env-laptop-relay`). 401 coverage is parametrized over list / dpop-token / status / connect / environment `/oauth/token` → `RelayNotAuthenticated`, body not in JSON. 200 non-JSON for list / dpop-token / connect → `T3ApiError`; connect without `credential` → `T3ApiError`. Mode-B-only `t3_environments` 401 is not “no environments configured” and names `hermes t3code connect`; `t3_list` on that path returns JSON and does not raise. Clerk-refresh retry is tested. WI-5 goldens and WI-7 FakeClock paths still pass after `_client_for` / `_ensure_discovered` / status-based mode-B liveness.
- SIMPLICITY: Met. Extra types (`DpopKey`, `DpopEnvClient`, the two cache records, `RelayNotAuthenticated`) are required: RFC 9449 proofs are per-method/URL (so a static `headers_fn` cannot carry them), FR-9 needs two-tier expiry, and mode-B 401 must name `connect` rather than `login`. No device-registration / live-activity surface, no OAuth framework. `auth_t3connect.py` holds WI-8 Clerk plus WI-9 relay in the files_scope the plan named; the WI-9 addition is the relay client + refresh loop, not a speculative layer.

## Defects

## Probes run
- `python3 -m pytest tests/test_dpop.py tests/test_relay.py tests/test_read_tools.py tests/test_wait.py -q` → 69 passed in 0.21s (6 + 22 + 21 + 20). WI-5 goldens (`t3_environments` / `t3_list` / `t3_thread`) and WI-7 FakeClock settle / approval / timeout paths still pass. `rg '401|malformed' tests/test_relay.py` hits the parametrized 401 suite plus Mode-B empty-config and `t3_list` JSON-not-raise tests. `rg 'time.sleep|sleep(' tests/test_dpop.py tests/test_relay.py` → none. Tests talk to `relay.example.test` / `desktop.example.test` via MockTransport; `https://relay.t3.codes` appears only as `DEFAULT_RELAY_URL`.
- `git status --short`: `M pyproject.toml`, `M talaria/auth_t3connect.py`, `M talaria/tools.py`, untracked `talaria/dpop.py`, `tests/test_dpop.py`, `tests/test_relay.py` (plus the round-1 critique file).
- JWT probe (cryptography ECDSA SHA-256 on compact r||s): `typ=dpop+jwt`, `alg=ES256`, `htm=POST`, `htu` strips query/fragment, `iat=100`, `jti` preserved, no `ath` without token, `ath` matches `base64url(sha256(token))` with token; JWK has no `d`; RFC 7638 thumbprint matches. `T3CODE_DPOP_KEY` store value has no `BEGIN`/`PRIVATE`. `talaria/dpop.py` has no top-level `cryptography` import.
- dpop-token form probe: `POST https://relay.example.test/v1/client/dpop-token`, no `Authorization`, form is exactly `{grant_type, subject_token, subject_token_type=urn:ietf:params:oauth:token-type:jwt, requested_token_type, resource=https://relay.example.test, scope=environment:connect environment:status, client_id=t3-web}`; proof header `typ=dpop+jwt` / `htm=POST` / `htu` that URL / no `ath`. Matches `packages/contracts/src/relay.ts` `RelayDpopAccessTokenRequest` and `managedRelay.ts` `exchangeAccessToken`.
- Refresh replay (`now=` injected): access still inside 5-minute skew with live credential → `/connect` 1, `/oauth/token` 2; `expires_in=3600`, `cred_ttl=100` → `/connect` 2, `/oauth/token` 2, `/v1/client/dpop-token` 1.
- Round-1 defect re-probe (independent of the test file helpers): Mode-B-only `handle_t3_environments` with Clerk set, no mode-A, `GET /v1/environments` → 401 `{"error":"LEAK-ME-TOKEN",…}` → `{"error":"not authenticated","hint":"run \`hermes t3code connect\` to sign in to T3 Connect"}`; no “no environments configured”, no leak, no `login` in the hint. Same 401 mapping for `list_environments` / `dpop_token` / `environment_status` / `connect_environment` / `ensure_environment_access` (oauth). List/dpop-token/connect 200 `not-json-oops` → `T3ApiError` status 200; connect 200 without `credential` → `T3ApiError` naming credential.
- Merge/status/collision re-probe: names `{laptop, studio, env-laptop-relay}`; `laptop` stays `direct`; `studio` `live=True` / `label=Studio Box` from status descriptor; colliding row `live=False`. Path list: `GET relay…/v1/environments`, `GET t3.example.test/.well-known/t3/environment`, `POST …/v1/client/dpop-token`, `POST …/v1/environments/env-laptop-relay/status`, `POST …/v1/environments/env-studio-1/status`. No `desktop.example.test` or `other.example.test` well-known. `resolve_environment(ctx, "env-laptop-relay")` is t3connect at `https://other.example.test`.
- `t3_list` Mode-B-only all-401 → JSON connect hint, no raise, no leak. After a successful list/connect/oauth, a later shell 401 still returns JSON and does not raise (hint on that path is still the WI-3 `login` pairing string from `T3EnvClient._parse`; not a round-1 remaining defect).
- Socket-deny (`socket.socket` / `create_connection` / `connect` after httpx+ssl loaded): wipe `talaria*`, `import talaria` + `register(Fake)` → 8 tools, CLI `t3code`. Handlers JSON on extra kwargs / junk environment arg.
- Checked against t3code `packages/shared/src/dpop.ts` (`typ`/`alg`/`htm`/`htu`/`iat`/`jti`/`ath`, compact ES256, RFC 7638), `dpopCommon.ts` `normalizeDpopHtu`, `packages/contracts/src/relay.ts` list/dpop-token/status/connect (`client_id` literals, `clientKeyThumbprint` deprecated alias), `packages/client-runtime/src/relay/managedRelay.ts`, `packages/client-runtime/src/authorization/remote.ts` `exchangeRemoteDpopAccessToken` (dpop header, bootstrap `subject_token_type`, no `ath` on the exchange), `infra/relay/src/auth/RelayTokens.ts` (`t3-web` allowed scopes = connect+status).
