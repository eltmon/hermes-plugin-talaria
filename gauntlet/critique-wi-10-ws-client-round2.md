# WI-10 ws-client — round 2
Verdict: PASS

## Criteria
- CORRECTNESS: Met. Round-1 defects 1 and 2 are gone on a fresh Ack-gated peer, not just `ScriptedWs`. After each incoming `Chunk`, [`talaria/t3_ws.py`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/t3_ws.py) `_dispatch` puts values on the stream queue then sends `{"_tag":"Ack","requestId"}` (same id). Independent latch peer (mirrors Effect `RpcServer.streamEffect`: write Chunk → `latch.await` → next Chunk *or* terminal `Exit`; Interrupt writes `Exit` immediately): subscribe received `[snapshot, synchronized]`, `hung=False`, `acks=[{"_tag":"Ack","requestId":"req-1"}]*2`, `chunks_sent=2`, `exit_sent=True`; sent tags `Ping, Request, Ack, Ack` (no Interrupt — Success `Exit` after the last Ack completed the iterator). Unary `server.probe` / `projects.listEntries` still round-trip. Encoder `_request_frame("probe-1","server.probe",{},None)` is `{"_tag":"Request","id":"probe-1","tag":"server.probe","payload":{},"headers":[]}`; handshake `https→wss` / `http→ws` on `/ws?wsTicket=…`. Encoded Interrupt is `{_tag, requestId}` only (`interruptor` absent from the client). `{"_tag":"Defect","defect":"boom"}` and `{"_tag":"ClientProtocolError","error":{…}}` fail in-flight waiters with `RpcProtocolError` (independent 0.4s timeout: `hung=False`). Expo backoff 1, 2, 4, … cap 30s via injected `sleep` (wall 0.0001s). Ticket rejection retries once with a fresh `ticket_fn()` and no backoff, then raises `TicketRejected`. CP-3 in [`docs/design.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/design.md) now documents Ack next to Ping/Pong/Interrupt, plus Defect / ClientProtocolError as terminal. FR-10 / NFR-1 / NFR-5 hold for this item.
- CONTRACT: Met. `import talaria` / `import talaria.t3_ws` / `T3WsClient(...)` / `register(FakeCtx)` open no sockets. `websockets` is imported only inside `_default_open_ws`, not at module import (`websockets` absent from `sys.modules` after `import talaria.t3_ws`). Constructor does not dial; `ticket_fn` runs per `_dial`. `register()` still does not import `t3_ws` (`talaria` has no `T3WsClient` attr). NFR-4 holds.
- SECURITY: Met. [`tests/fixtures/frames/transcript.json`](/home/eltmon/Projects/hermes-plugin-talaria/tests/fixtures/frames/transcript.json) has no token / Bearer / secret / wsTicket / PEM / JWT (payloads are `/tmp/demo` + `README.md`). Tests use `tix-1` / `tix-2` as query values only. Client does not log the ticket. NFR-2 is not in play (no secret store on this item). No listener.
- TESTS: Met. [`tests/test_ws.py`](/home/eltmon/Projects/hermes-plugin-talaria/tests/test_ws.py) `ScriptedWs` now latches: first shell `Chunk` is pushed, remaining chunks sit in `_rest_chunks` until `Ack` (`_release_after_ack`). Transcript order is Request → Chunk → Ack → Chunk → Ack → Interrupt → Exit. `test_subscribe_shell_chunks_then_interrupt` and `test_close_interrupts_open_subscribe` assert `acks == [{"_tag":"Ack","requestId": req["id"]}] * len(SHELL_CHUNKS)`. Unary probe/listEntries, NFR-5 unknown Exit fields, `RpcExitFailure` on missing `cwd`, backoff with `ImmediateClock` + boom-patched `asyncio.sleep` (`[1,2,4,8,16,30,30]`, 8 attempts), 2-try ticket-rejection latch, HTTP 401 upgrade, drop-then-redial, Defect / ClientProtocolError waiter tests all still present. Clock injection is real: independent replay sleeps `[1.0, 2.0, 4.0]`, wall 0.0001s. A missing Ack now stalls the fake before chunk 2, so AC2 cannot false-green the round-1 hang.
- SIMPLICITY: Met. 361 lines vs hermes `gateway/relay/ws_transport.py` (~1129): one client, injected `open_ws`/`sleep`, request-id maps, no supervisor task, no hello/descriptor handshake, no drain. `RpcProtocolError` is the one new type the Defect/CPE fix needs. `now=` is stored and never read; leftover, not a new layer.

## Defects

## Probes run
- `python3 -m pytest tests/test_ws.py tests/test_scaffold.py -q` → 21 passed in 0.12s. `python3 -m pytest tests/ -q` → 203 passed in 0.32s.
- `git status --short`: `M .gitignore`, `M docs/design.md`, `M pyproject.toml` (`websockets` added), untracked `talaria/t3_ws.py`, `tests/test_ws.py`, `tests/fixtures/frames/`, round-1 critique.
- Encoder: `_request_frame("probe-1","server.probe",{},None)` → `{"_tag":"Request","id":"probe-1","tag":"server.probe","payload":{},"headers":[]}`. `_ws_url` https→`wss://t3.example.test/ws?wsTicket=tix-1`, http→`ws://127.0.0.1:3773/ws?wsTicket=tix`. `interruptor` not in [`talaria/t3_ws.py`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/t3_ws.py).
- Socket-deny (`socket.socket` / `create_connection` / `connect` after httpx+ssl loaded): wipe `talaria*`, `import talaria` + `import talaria.t3_ws` + `T3WsClient(...)` + `register(FakeCtx)` → 8 tools + CLI `t3code`, `websockets` not in `sys.modules`, `hasattr(talaria, "T3WsClient")` is False.
- Independent Ack-gated peer (not `ScriptedWs`; latch after every Chunk including before Success Exit): `hung=False`; items=`[snapshot, synchronized]`; sent tags=`['Ping','Request','Ack','Ack']`; `acks=[{requestId: req-1}]*2`; `chunks_sent=2`; `exit_sent=True`. Round-1 probe on the same shape was `items=[snapshot]`, `acks=[]`, `chunks_sent=1`, `exit_sent=False`, hung.
- Defect peer: unary `request()` + `{"_tag":"Defect","defect":"boom"}` → `RpcProtocolError(tag=Defect, payload=boom)` in <0.4s (round-1 hung 0.3s). ClientProtocolError peer: `tag=ClientProtocolError`, payload `{"reason":"x"}`, not hung.
- Backoff replay (injected sleep, 3 failures then success): sleeps `[1.0, 2.0, 4.0]`, attempts 4, wall 0.0001s. Ticket-rejection latch: first `request()` opens=2 then `TicketRejected` / `auth_latched=True` / sleeps `[]`; a second `request()` opens=4 more — the flag is still not consulted (hermes `_auth_revoked` is; T3 tickets are 5-min and re-mintable). Residual, not a blocker. Incoming server `Ping` is still not Pong’d (Effect server only *answers* client Pings; residual).
- Fixtures: no token/Bearer/secret/wsTicket/PEM/JWT. Transcript tags: out Ping, in Pong, out Request, in Exit, out Request, in Chunk, out Ack, in Chunk, out Ack, out Interrupt, in Exit, out Request, in Exit. `serverVersion` 0.0.37.
- Checked against t3code `RpcSerialization.layerJson` (one JSON object per WS text message), `RpcMessage.RequestEncoded` / `ResponseChunkEncoded` / `AckEncoded` / `InterruptEncoded` / `Ping`/`Pong`, `RpcServer.ts` `streamEffect` (`latch.closeUnsafe` + `latch.await` after each Chunk write; Ack `latch.open`; Interrupt `fiber.interruptUnsafe`) + `makeSocketProtocol.supportsAck: true`, `apps/server/src/ws.ts` `toHttpEffectWebsocket(WsRpcGroup)`, official `RpcClient.ts` Ack-after-Chunk (~559–571) and Defect/ClientProtocolError failing entries. hermes reconnect bar: `ping_interval=30`, `ping_timeout=60`, expo backoff cap 30s — this client copies those; WS-level ping is the PRD 30s interval.
