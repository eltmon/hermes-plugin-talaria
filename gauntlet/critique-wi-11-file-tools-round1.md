# WI-11 file-tools — round 1
Verdict: PASS

## Criteria
- CORRECTNESS: Met. The four tools speak the t3code `packages/contracts/src/project.ts` RPCs field-for-field over the WI-10 WS client: `t3_ls` → `projects.listEntries` `{cwd}`; `t3_read_file` → `projects.readFile` `{cwd, relativePath}`; `t3_write_file` → `projects.writeFile` `{cwd, relativePath, contents}`; `t3_search` → `projects.searchContents` `{cwd, query, limit, caseSensitive, wholeWord, useRegex}`. Independent replay of the handlers produced those exact dicts (search query `" foo"` kept untrimmed; omitted flags sent `false`; omitted limit sent `50`; `limit=9999` capped at `500` = `PROJECT_SEARCH_CONTENTS_MAX_LIMIT`). `cwd` is the project's `workspaceRoot` from the shell snapshot (optional `t3_ls` `path` is joined into `cwd` because listEntries has no `relativePath`). Reads cap at `256 * 1024` bytes with `"truncated": true` (ascii + UTF-8 `€` at the cut both ≤ 262144; exact 256 KiB stays `truncated: false`). Writes refuse missing/blank `project_id` or `path` (and missing `contents`) with no RPC. `RpcExitFailure` whose cause is `EnvironmentAuthorizationError` (Fail object or Sequential list) returns JSON that names the missing scope: `orchestration:read` for ls/read/search, `orchestration:operate` for write — matching [`RpcAuthorization.ts`](/home/eltmon/Projects/t3code/apps/server/src/auth/RpcAuthorization.ts). Unknown result fields pass through (NFR-5). FR-10 / NFR-3 hold for this item.
- CONTRACT: Met. Handlers are `(args: dict, **kwargs) -> str`, always JSON, never raise (`RpcExitFailure` → `_rpc_failure_json`, `TalariaError.to_json()`, junk args / factory boom → `_unexpected`). Extra kwargs accepted. Missing ctx (or no `bind_ctx`) still returns the WI-1 stub JSON. [`plugin.yaml`](/home/eltmon/Projects/hermes-plugin-talaria/plugin.yaml) `provides_tools` now ends `t3_ls`, `t3_read_file`, `t3_write_file`, `t3_search`; `register(FakeCtx)` registers those 12 names in the same order. Import / register / `T3WsClient(...)` open no sockets; `websockets` stays out of `sys.modules` after `import talaria`; `talaria` has no `T3WsClient` attr. NFR-4 holds.
- SECURITY: Met. Tool JSON, fixtures under [`tests/fixtures/files/`](/home/eltmon/Projects/hermes-plugin-talaria/tests/fixtures/files/), and authz errors never contain the bearer (`tok-test` absent; fixtures have no token / Bearer / wsTicket / PEM / JWT). 401 / missing secret maps to `NotAuthenticated` and drops the body. Writes cannot fire without an explicit project-relative path. NFR-1 (no listener) and NFR-2 (no secret store on this item) hold. Hermes dispatch kwargs are `task_id`/`session_id`/`user_task`, not `cwd`; the `kwargs["cwd"]` branch is a test seam, not a host-cwd override on the live path.
- TESTS: Met. AC1 is [`tests/test_file_tools.py`](/home/eltmon/Projects/hermes-plugin-talaria/tests/test_file_tools.py): one fixture-driven success RPC per tool (`list_entries.success.json` / `read_file.success.json` / `write_file.success.json` / `search_contents.success.json`) plus `test_authorization_error_names_missing_scope` parametrized over all four tags with `authz_read.json` / `authz_write.json` (`requiredScope` in JSON and in `error`, `scope` in `hint`). Read-cap test asserts `truncated is True` and contents ≤ `READ_BYTE_LIMIT`. `test_write_requires_project_id_and_path` asserts the three missing-field errors and `fake.calls == []`. Fake WS client only (no live T3); shell cwd resolution uses `httpx.MockTransport`; no `sleep`. Independent probe reproduced payload shapes, cap, authz, never-raise, and stub paths without going through those tests.
- SIMPLICITY: Met. No extra module — files_scope is [`talaria/schemas.py`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/schemas.py), [`talaria/tools.py`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/tools.py), [`tests/test_file_tools.py`](/home/eltmon/Projects/hermes-plugin-talaria/tests/test_file_tools.py), plus the required `plugin.yaml` / `__init__.py` / conftest factory reset. One `_file_tool` plus a small cause walker (the AC needs `EnvironmentAuthorizationError` out of Effect Fail/Sequential). Per-call WS open/close, no connection pool, no pydantic. `tools.py` is 1115 lines because it still owns every earlier tool; the WI-11 append is the four handlers and their helpers, not a new layer. Spotify `tools.py` is 454 as the per-handler shape bar.

## Defects

## Probes run
- `python3 -m pytest tests/test_file_tools.py tests/test_scaffold.py -q` → 29 passed in 0.14s. `python3 -m pytest tests/ -q` → 226 passed in 0.38s. `git status --short`: `M plugin.yaml`, `M talaria/__init__.py`, `M talaria/schemas.py`, `M talaria/tools.py` (+445, append-only after `handle_t3_wait`), `M tests/conftest.py` (`set_ws_client_factory` reset), untracked `tests/test_file_tools.py`, `tests/fixtures/files/`.
- Wire payloads (independent FakeWs, not the builder tests): `projects.listEntries` `{cwd}`; `projects.readFile` `{cwd, relativePath}`; `projects.writeFile` `{cwd, relativePath, contents}`; `projects.searchContents` `{cwd, query: " foo", limit: 50, caseSensitive: false, wholeWord: false, useRegex: false}`. `READ_BYTE_LIMIT == 262144`; ascii over-cap → `truncated=True`, 262144 bytes; UTF-8 `€` cut → 262143 bytes valid UTF-8; exact 256 KiB → `truncated=False`. Write missing `project_id` / `path` / `contents` / blank path → those errors, `calls=[]`.
- Authz: Fail `{_tag: EnvironmentAuthorizationError, requiredScope: orchestration:read}` → JSON `requiredScope` + scope in `error` + `scope` in `hint`; Sequential list form for write → `orchestration:operate`. Token `tok-test` absent from JSON. Junk args (`None`/`str`/`int`) and missing ctx → JSON, no raise.
- Socket-deny after httpx+ssl loaded: wipe `talaria*`, `import talaria` + `register(FakeCtx)` → 12 tools matching `plugin.yaml`, CLI `t3code`, `websockets` not in `sys.modules`, `hasattr(talaria, "T3WsClient")` is False. `T3WsClient(...)` does not dial (`_ws is None`).
- Fixtures: no tok-/Bearer/secret/wsTicket/PEM/JWT. Scopes match t3code `RPC_REQUIRED_SCOPES` (`listEntries`/`readFile`/`searchContents` → `orchestration:read`; `writeFile` → `orchestration:operate`). Input structs checked against `ProjectListEntriesInput` / `ProjectReadFileInput` / `ProjectWriteFileInput` / `ProjectSearchContentsInput` in [`/home/eltmon/Projects/t3code/packages/contracts/src/project.ts`](/home/eltmon/Projects/t3code/packages/contracts/src/project.ts). t3code server read cap is 1 MiB; the plugin’s 256 KiB cap is client-side as specified (RPC has no maxBytes field).
