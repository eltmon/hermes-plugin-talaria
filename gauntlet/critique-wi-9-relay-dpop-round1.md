# WI-9 relay-dpop — round 1
Verdict: NOT_YET

## Criteria
- CORRECTNESS: Not met. AC1/AC2 happy paths work: ES256 compact proofs verify (`typ=dpop+jwt`, `alg=ES256`, `jwk` without `d`, `htm`/`htu`/`iat`/`jti`, `ath` = unpadded base64url(SHA-256(token)) only when an access token is present); RFC 7638 thumbprint is SHA-256 of canonical `{crv,kty,x,y}`; `POST /v1/client/dpop-token` form matches t3code `RelayDpopAccessTokenRequest` (`grant_type` token-exchange, `subject_token_type=urn:ietf:params:oauth:token-type:jwt`, `resource` = relay origin, `scope=environment:connect environment:status`, `client_id=t3-web` — a legal `t3-web|t3-mobile` literal, and `t3-web` is the client that is allowed exactly those two scopes). Independent replay of `ensure_environment_access` with injected `now=`: first call does connect+exchange; `now+50` (access still inside the 5-minute skew) is a cache hit; `now+(3600-300)+1` with `cred_ttl=100` does a second connect and a second `/oauth/token`. AC3 merge lists `{laptop: direct, studio: t3connect}` and caches discovered rows in `ctx.state`. Those paths are not enough for FR-1/FR-12. `_ensure_discovered` in [`talaria/tools.py:138`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/tools.py:138) swallows every exception from `GET /v1/environments`, so a Mode-B-only Clerk 401 becomes `{"error":"no environments configured","hint":"… hermes t3code login …"}` — no auth status, the wrong ceremony. `_probe_env` still uses unauthenticated `GET /.well-known/t3/environment` for `live`; WI-5 deferred `POST /v1/environments/:id/status` to this item (PRD WI-5 “mode B adds relay status”, design.md `t3_environments` row) and `environment_status()` is never called from the tool. Independent merge probe’s path list is list + two descriptor GETs, zero `/status`. `resolve_environments` drops a discovered env whose label collides with a mode-A name, so `environment=env-laptop-relay` is `EnvironmentNotFound` — not a union keyed by label and `environmentId` (FR-12).
- CONTRACT: Met. `import talaria` / `talaria.dpop` / `talaria.auth_t3connect` / `talaria.tools` plus `register(FakeCtx)` open no sockets (`socket_guard` and a live constructor-deny after httpx/ssl are loaded). `get_client()` stays lazy; cryptography is imported only inside key/sign functions, not at module import. Handlers still `(args, **kwargs) -> str` JSON and never raise: `TalariaError.to_json()` plus `_unexpected`. Probe: Mode-B `t3_list` 401, junk `args` (`None`/`str`/`int`), extra kwargs all return JSON. Missing ctx (or scaffold Fake without `get_config` for wait/write) still hits the WI-1 stub path.
- SECURITY: Met for NFR-2 / Decision 7. `T3CODE_DPOP_KEY` is base64(PKCS8 PEM) via `set_secret`, never raw PEM, never `ctx.set_config`. Clerk access stays `T3CODE_CLERK_ACCESS_TOKEN`. Probe: config blob and `config_writes` contain neither the Clerk fake nor the DPoP secret; tool JSON for 401 drops the body (`LEAK-ME` / `tok-clerk-access` absent). Tests use `tok-clerk-access` / generated in-memory keys only. Fixtures have no PEM. Public `pk_live_…` and `DEFAULT_RELAY_URL` are identifiers, not secrets.
- TESTS: Not met. [`tests/test_dpop.py`](/home/eltmon/Projects/hermes-plugin-talaria/tests/test_dpop.py) + [`tests/test_relay.py`](/home/eltmon/Projects/hermes-plugin-talaria/tests/test_relay.py) are MockTransport / injected `now=` / no `time.sleep` / no live relay — that part is right — but there is zero 401 or malformed coverage (`rg 401|malformed` is empty in both files). The Mode-B-only Clerk 401 → “no environments configured” lie is therefore untested, as are connect/dpop-token/list malformed JSON, missing `credential`, and `t3_list`/`t3_environments` JSON-not-raise on the relay 401 path. AC2 clock tests exist; they do not substitute for the 401/malformed floor every prior HTTP item had (WI-3/WI-5/WI-7).
- SIMPLICITY: Met. Extra types (`DpopKey`, `DpopEnvClient`, the two cache records) are required: RFC 9449 proofs are per-method/URL (so a static `headers_fn` cannot carry them) and FR-9 needs two-tier expiry. No device-registration / live-activity surface, no OAuth framework. `auth_t3connect.py` is 867 lines because WI-8 Clerk plus WI-9 relay share the file (spotify `client.py` is 435); the WI-9 addition is the relay client + refresh loop, not a speculative layer.

## Defects
1. **Relay 401/malformed is untested, and `_ensure_discovered` turns a Mode-B 401 into “no environments configured”.** [`talaria/tools.py:138`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/tools.py:138) `except Exception: return`. Independent probe: Clerk token set, no mode-A entries, `GET /v1/environments` → 401 with `{"token":"tok-clerk-access-LEAK"}` → `handle_t3_environments` returns `{"error":"no environments configured","hint":"add a mode-A entry … or run \`hermes t3code login <pairing-url>\`"}`. The 401 never surfaces; the hint names the wrong ceremony; FR-1 auth status is a lie. Fix: do not swallow `NotAuthenticated` / `T3ApiError` when the resolved set is empty — return that JSON. If a Clerk refresh token is stored, call WI-8 `refresh_clerk_tokens` once and retry list/dpop-token (Clerk access is short-lived; this is the first consumer of that token). Change `NotAuthenticated()` raised from `_clerk_access` so the hint is `hermes t3code connect`, not `login`. Add MockTransport tests: list / dpop-token / status / connect / environment `/oauth/token` 401 → `NotAuthenticated`, body not in JSON; 200 with non-JSON or connect without `credential` → `T3ApiError`; Mode-B-only `t3_environments` 401 is not “no environments configured”; `t3_list` on a discovered env returns JSON and does not raise.

2. **Mode-B `live` still ignores relay status.** [`talaria/tools.py:310`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/tools.py:310) `_probe_env` always does unauthenticated descriptor GET via `make_env_client`. `environment_status()` in [`talaria/auth_t3connect.py:361`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/auth_t3connect.py:361) is implemented and unit-tested, then unused by `t3_environments`. WI-5 literally deferred `POST /v1/environments/:id/status` to WI-9; PRD WI-5 says “liveness (mode B adds relay status)”; design.md’s `t3_environments` row is “per-env descriptor + POST /v1/environments/:id/status liveness”. The merge test’s catch-all is `404` for anything that is not list or descriptor — if status were wired that test would fail. Independent path list: `GET relay…/v1/environments`, `GET t3.example.test/.well-known/t3/environment`, `GET desktop.example.test/.well-known/t3/environment`, no `/status`. Fix: for `ref.mode == "t3connect"`, `live = (environment_status(…).get("status") == "online")`; use the status payload’s optional `descriptor` for `label`/`environmentId` when present. Mock `POST /v1/environments/:id/status` in the merge test (online + offline rows). Do not use tunnel descriptor timeout as Mode-B liveness.

3. **FR-12 union is name-keyed only; a colliding T3 Connect environment disappears.** [`talaria/auth_t3connect.py:489`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/auth_t3connect.py:489) `discovered_environment_map` keys by slugified label (`laptop`) and only falls back to `environmentId` when the collision is among discovered rows. [`talaria/config.py:92`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/config.py:92) then skips any discovered name already in the mode-A map. Probe: mode-A `laptop` + discovered `{environmentId: env-laptop-relay, label: laptop}` → `resolve_environment(ctx, "env-laptop-relay")` is `EnvironmentNotFound`. PRD config paragraph: resolved set is the union of mode-A and mode-B, keyed by label **and** `environmentId`. Fix: when the slug is already taken by a mode-A entry (or another discovered row with a different id), store the discovered ref under `environmentId` so `environment=env-laptop-relay` resolves. Keep mode-A `laptop` as `direct`. Update the merge test: visible names may still be `{laptop, studio, env-laptop-relay}` (or equivalent); lookup by the dropped id must succeed.

## Probes run
- `python3 -m pytest tests/test_dpop.py tests/test_relay.py tests/test_read_tools.py tests/test_wait.py -q` → 56 passed in 0.37s (6 + 9 + 21 + 20). WI-5 goldens and WI-7 FakeClock paths still pass after `_client_for` / `_ensure_discovered`. `rg '401|malformed' tests/test_dpop.py tests/test_relay.py` → no matches. `rg 'time.sleep|sleep(' tests/test_dpop.py tests/test_relay.py` → none (only `urllib.parse.parse_qs` and a `BEGIN` assertion). Tests talk to `relay.example.test` / `desktop.example.test` via MockTransport; `https://relay.t3.codes` appears only as `DEFAULT_RELAY_URL`.
- `git status --short`: `M pyproject.toml` (`cryptography` added; already in `plugin.yaml` `python_dependencies`), `M talaria/auth_t3connect.py`, `M talaria/tools.py`, untracked `talaria/dpop.py`, `tests/test_dpop.py`, `tests/test_relay.py`.
- JWT probe (cryptography ECDSA SHA-256 on compact r||s): `typ=dpop+jwt`, `alg=ES256`, `htm=POST`, `htu` strips query/fragment, `iat=100`, `jti` preserved, no `ath` without token, `ath` matches `base64url(sha256(token))` with token; JWK has no `d`; RFC 7638 thumbprint matches. `T3CODE_DPOP_KEY` store value has no `BEGIN`/`PRIVATE`.
- dpop-token form probe: `POST https://relay.example.test/v1/client/dpop-token`, no `Authorization`, form is exactly `{grant_type, subject_token, subject_token_type=urn:ietf:params:oauth:token-type:jwt, requested_token_type, resource=https://relay.example.test, scope=environment:connect environment:status, client_id=t3-web}`; proof header `typ=dpop+jwt` / `htm=POST` / `htu` that URL / no `ath`. Matches `packages/contracts/src/relay.ts` `RelayDpopAccessTokenRequest` and `managedRelay.ts` `exchangeAccessToken` (source: dpop header + subject_token, not a Clerk `Authorization` bearer — the PRD table is the one that is wrong).
- Refresh replay (`now=` injected, `expires_in=3600`, `cred_ttl=100`): `/connect` 2, `/oauth/token` 2, `/v1/client/dpop-token` 1 — re-exchange after skew, re-connect after credential expiry.
- 401/malformed probe: `list_environments` / `dpop_token` / `connect_environment` 401 → `NotAuthenticated` (body dropped); connect 200 without `credential` → `T3ApiError` “relay connect returned no credential”; list 200 `not-json{` → `T3ApiError`. `handle_t3_environments` Mode-B-only 401 → **“no environments configured”** (swallowed). `handle_t3_list` discovered studio + 401 → JSON `not authenticated` / login hint, no raise, no `LEAK-ME`.
- Merge/status probe: `live` for studio is True from descriptor GET; `/status` never called. Collision probe: `env-laptop-relay` not found.
- Socket-deny (`socket.socket` / `create_connection` / `connect` after httpx+ssl loaded): `import talaria` + `register(Fake)` → 8 tools, CLI `t3code`. Handlers JSON on missing ctx / extra kwargs.
- Checked against t3code `packages/shared/src/dpop.ts` (`typ`/`alg`/`htm`/`htu`/`iat`/`jti`/`ath`, compact ES256, RFC 7638), `dpopCommon.ts` `normalizeDpopHtu`, `packages/contracts/src/relay.ts` list/dpop-token/status/connect (`client_id` literals, `clientKeyThumbprint` deprecated alias still sent by `managedRelay.ts` and accepted in `infra/relay/src/http/Api.ts` as `clientKeyThumbprint ?? clientProofKeyThumbprint`), `packages/client-runtime/src/authorization/remote.ts` `exchangeRemoteDpopAccessToken` (dpop header, bootstrap `subject_token_type`, no `ath` on the exchange).
