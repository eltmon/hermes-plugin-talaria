# WI-13 docs — round 2
Verdict: PASS

## Criteria
- CORRECTNESS: Met. Round-1 defect 1 is gone on a fresh probe, not just the rewritten sentence. [`docs/design.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/design.md) CP-4 now says the 2026-08-31 404 is **not** closed; it does not claim [NousResearch/hermes-plugin-index](https://github.com/NousResearch/hermes-plugin-index) is live. Independent probe 2026-09-01: `curl` of `https://raw.githubusercontent.com/NousResearch/hermes-plugin-index/main/index.json` is HTTP 404 (`404: Not Found`); GitHub page 404; `gh repo view NousResearch/hermes-plugin-index` is `Could not resolve to a Repository`. Live HTTP 200 is [Revell-ai/hermes-plugin-index](https://github.com/Revell-ai/hermes-plugin-index) (`https://raw.githubusercontent.com/Revell-ai/hermes-plugin-index/main/index.json`, JSON `plugins` list, 9 entries). Hermes [`hermes_cli/plugin_index.py`](/home/eltmon/Projects/hermes-agent/hermes_cli/plugin_index.py) `DEFAULT_INDEX_URL` is still the Nous raw URL, so a stock client still falls back to the bundled seed unless `plugins.index_url` is pointed at Revell-ai. CP-4, [`docs/plugin-index-entry.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/plugin-index-entry.md), and README “Community index” all record that split: Nous is the code default and still 404s; Revell-ai is the working PR target; Discord `#plugins-skills-and-skins` plus seed `hermes_cli/data/plugin_index.json` remain the fallback; `ref` stays `REPLACE_WITH_HEAD_SHA`; do not submit from this item. AC1 still holds: every `plugin.yaml` `config_schema` key and every `T3CODE_*` secret name in `talaria/` appears in [`README.md`](/home/eltmon/Projects/hermes-plugin-talaria/README.md). Schema keys `t3connect` (`enabled`, `relay_url`, `hosted_app_url`, `clerk_publishable_key`, `clerk_oauth_client_id`), `environments` (`{ base_url }`), `default_environment`, `allow_gateway_injection`; secrets `T3CODE_TOKEN_<NAME>` (mangling `my-laptop` → `T3CODE_TOKEN_MY_LAPTOP`), `T3CODE_CLERK_ACCESS_TOKEN`, `T3CODE_CLERK_REFRESH_TOKEN`, `T3CODE_CLERK_PKCE_PENDING`, `T3CODE_DPOP_KEY`. `get_config`/`set_config` literals are `t3connect` / `environments` / `default_environment` / `allow_gateway_injection` — same set. Nested env parser still reads `environment_id` / `environmentId` / `mode`; README documents `{ base_url }` and lookup by `environmentId` / mode A vs B — not a missing schema key. Walkthrough install → enable → login → `t3_prompt` has no extra config/env/secret-export step.
- CONTRACT: Met for the docs surfaces. [`README.md`](/home/eltmon/Projects/hermes-plugin-talaria/README.md) has install/enable, both auth walkthroughs, config table, injection note, tested t3code **0.0.37**, and the rewritten Community index block. [`CONTRIBUTING.md`](/home/eltmon/Projects/hermes-plugin-talaria/CONTRIBUTING.md) has venv setup, `python -m pytest tests/ -q`, `hermes plugins doctor . --ci`. [`docs/PRD.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/PRD.md) status is `M1+M2 implemented · 2026-09-01`. [`docs/plugin-index-entry.json`](/home/eltmon/Projects/hermes-plugin-talaria/docs/plugin-index-entry.json) matches `PluginIndexEntry` / seed `hermes_cli/data/plugin_index.json` (`name`, `repo` `owner/name`, `ref`, optional `homepage`/`tags`/`capabilities`/`api_version`/`added_at`; no extra fields). `name: t3code` matches [`plugin.yaml`](/home/eltmon/Projects/hermes-plugin-talaria/plugin.yaml). Placeholder SHA is still allowed.
- SECURITY: Met. README still tells operators tokens never go in `config.yaml` / `plugin-data/` / the install dir; secret table names `~/.hermes/.env`. Clerk pk/client id stay labeled public identifiers. No new secret names in code that the README omits. NFR-2 documentation holds for this item.
- TESTS: Met as a docs item’s mechanical check. `python3 -m pytest tests/ -q` → 242 passed in 0.38s. Config/secret inventory was a code-vs-README grep, not a copy of the README. CP-4 was probed against live GitHub/raw URLs and `gh`, not against the design.md sentence. No `time.sleep` added.
- SIMPLICITY: Met. files_scope is README / CONTRIBUTING / `docs/PRD.md` / `docs/design.md`, plus the two draft index files CP-4 needs. Round-2 edit is the CP-4 rewrite plus matching README / `plugin-index-entry.md` wording. No extra onboarding framework.

## Defects

## Probes run
- `python3 -m pytest tests/ -q` → 242 passed in 0.38s. `git status --short`: `M README.md`, `M docs/PRD.md`, `M docs/design.md`, untracked `CONTRIBUTING.md`, `docs/plugin-index-entry.json`, `docs/plugin-index-entry.md`, round-1 critique. `git diff --stat`: README +256, PRD status one-line flip, design +21 (CP-4 section). design.md has no `404 is closed` / `404 is **closed**`; CP-4 opens “The 2026-08-31 404 is **not** closed.”
- Config/secret inventory (`plugin.yaml` `config_schema` + `get_config`/`set_config` + `T3CODE_*` in `talaria/` vs README): all schema keys and all five secret names present. Nested t3connect keys `enabled` / `relay_url` / `hosted_app_url` / `clerk_publishable_key` / `clerk_oauth_client_id` all in the README table. Nested env parser also reads `environment_id` / `environmentId` / `mode`; README documents `{ base_url }` and lookup by `environmentId` / mode A vs B — not a missing secret/config-schema key.
- CP-4 live (independent of the docs sentence): `curl` `https://raw.githubusercontent.com/NousResearch/hermes-plugin-index/main/index.json` → HTTP 404, body `404: Not Found`. GitHub page HTTP 404. `gh repo view NousResearch/hermes-plugin-index` → `Could not resolve to a Repository`. `curl` `https://raw.githubusercontent.com/Revell-ai/hermes-plugin-index/main/index.json` → HTTP 200, JSON `{plugins: [...]}` with 9 entries. `gh repo view Revell-ai/hermes-plugin-index` resolves; contributing is “open a PR that adds an entry to `index.json`”; clients set `plugins.index_url` to that raw URL. Hermes `DEFAULT_INDEX_URL` is still the Nous raw URL. Draft entry parses; required `name`/`repo`/`ref` present; `repo` has one slash; no fields outside `PluginIndexEntry`.
- Walkthrough vs Hermes installer (unchanged from round 1, still holds): `hermes plugins install <owner/repo>` and `enable` exist; GitHub `eltmon/hermes-plugin-talaria` is public (no index needed). [`talaria/__init__.py`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/__init__.py) `register()` does not import `cryptography` (dpop lazy-imports on first T3 Connect DPoP use). Login path needs no `config.yaml` edit; `login()` upserts `environments.<name> = {base_url}` and `set_secret(T3CODE_TOKEN_…)`. `t3_prompt` required args are `thread_id` + `text` (walkthrough names both). Pairing and connect ceremonies remain verbatim (`hermes t3code login <pairing-url>`, `hermes t3code connect`, `hermes t3code connect --code '…'`).
- Residuals, not blockers: design.md “Hermes plugin shape” still names `NousResearch/hermes-plugin-index` as the community index, and the M3 checklist still has an unchecked “Submit to `NousResearch/hermes-plugin-index`” item. CP-4 + README + `plugin-index-entry.md` are the live-location record and contradict that as a submit-now step. Walkthrough still says `t3 pair` (PRD wording) not t3code’s `npx t3 pair`; “start the server” has no `npx t3 serve`; a loopback pairing URL is not reachable from another host; empty-thread `t3_new_thread` needs `project_id`/`title`/`model_selection` not spelled in the shortest path; session start command is “Start a Hermes session” not `hermes`.
