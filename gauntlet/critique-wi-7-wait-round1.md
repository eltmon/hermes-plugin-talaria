# WI-7 wait — round 1
Verdict: PASS

## Criteria
- CORRECTNESS: Met. CP-1 is recorded in [`docs/design.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/design.md) (`## CP-1 — t3_wait poll endpoint (decided fallback)`): `t3_wait` polls `GET /api/orchestration/shell` and reads that thread's `latestTurn` plus `hasPendingApprovals` / `hasPendingUserInput`; it does not poll thread detail. No live server confirmation, so the PRD-decided fallback is the live path. That matches t3code `packages/contracts/src/orchestration.ts`: `OrchestrationLatestTurn.state` is `running | interrupted | completed | error`; pending flags live on `OrchestrationThreadShell` only — `OrchestrationThread` / `OrchestrationThreadDetailSnapshot` have `latestTurn` but not the pending flags, so FR-6 ("settle or block on approval") cannot be served from thread detail alone. `handle_t3_wait` calls `client.shell()` inside the loop with a `TODO(CP-1)` thread-detail variant left in [`talaria/tools.py`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/tools.py). Terminal mapping: pending approval → `status=approval` (wins over user-input and settled), pending user-input → `user-input`, `latestTurn.state == "running"` or missing/null latestTurn → keep polling, `completed|interrupted|error` (and any other non-empty non-running string) → `settled`, deadline hit → `timeout`. Interval default 5s, floor `max(2, interval)`; timeout default 300s, clamped `>= 0`. First observation is immediate; sleep is `min(interval, remaining)` so the last wait does not overshoot. Payload is condensed terminal state + excerpt (`planProgress.step` if present, else `session.lastError`, else `state turnId`, else title), not the raw shell row.
- CONTRACT: Met. `handle_t3_wait(args, **kwargs) -> str` always returns JSON, never raises (`TalariaError.to_json()` plus `_unexpected`). Extra kwargs (`extra=True`, hermes `parent_agent`) are accepted. Missing ctx, or scaffold `FakeCtx` without `get_config` (`_ready_ctx`), still returns the WI-1 stub JSON so `tests/test_scaffold.py` / `tests/test_read_tools.py` stub assertions keep passing. Missing/non-string `thread_id` is `{error, hint}` JSON. Import of `talaria.tools` and `register(ctx)` open no sockets.
- SECURITY: Met. Success JSON is `environment` / `threadId` / `status` / condensed `latestTurn {turnId, state}` / pending booleans / excerpt — no bearer, no `extraTurnField` / `unexpectedField`. 401 maps to `NotAuthenticated` and drops the body (a 401 JSON that itself contains the token does not leak). Tests use `tok-test` only as the Authorization header plus that 401-body check. Non-401 `T3ApiError` still excerpts the HTTP body (WI-2 contract, same as WI-3/WI-5/WI-6) — the wait handler does not copy the request token into output.
- TESTS: Met. AC1 is the CP-1 section in `docs/design.md`. AC2 is [`tests/test_wait.py`](/home/eltmon/Projects/hermes-plugin-talaria/tests/test_wait.py): `test_settle_completed_after_running` (scripted running→running→completed), `test_approval_interrupt_returns_immediately`, `test_timeout_while_running`, plus interrupted/error settle, user-input interrupt, approval-wins, interval floor, remainder sleep, null latestTurn / missing thread keep polling. Clock is `FakeClock` injected as `sleep=` / `monotonic=` kwargs; autouse patches `time.sleep` and `talaria.tools.time.sleep` to raise. `rg time.sleep tests/test_wait.py` hits only the boom patch and the assertion string — no `time.sleep(` call, no real wait. Transport is `httpx.MockTransport` against `https://t3.example.test` (no live server). The 6-line deletion in `tests/test_write_tools.py` (`test_t3_wait_stays_stub_even_with_ctx`) is the allowed follow-on so the new implementation is not blocked by a prior stub assertion.
- SIMPLICITY: Met. Same shape as hermes [`plugins/spotify/tools.py`](/home/eltmon/Projects/hermes-agent/plugins/spotify/tools.py): per-tool `try/except`, JSON string, `**kwargs`. Wait is a `while True` over `client.shell()` plus small helpers (`_wait_interval` / `_wait_timeout` / `_thread_from_shell` / `_wait_terminal_status` / `_wait_payload` / `_activity_excerpt`) — not a scheduler, not a reducer, not a second HTTP client. Clock injection reuses the handler's `**kwargs` (hermes `registry.dispatch` already forwards kwargs) instead of a new module. Diff is `talaria/tools.py` + `tests/test_wait.py` + the CP-1 `docs/design.md` section required by AC1. `tools.py` is 590 lines vs spotify `tools.py` 454 because it already hosts the M1 read+write set; the wait addition is ~140 lines.

## Defects

## Probes run
- `python3 -m pytest tests/test_wait.py -q` → 20 passed in 0.10s. `python3 -m pytest tests/test_wait.py tests/test_write_tools.py tests/test_scaffold.py tests/test_read_tools.py -q` → 72 passed in 0.18s. `python3 -m pytest tests/ -q` → 160 passed in 0.33s (whole suite; no real sleeps).
- `git status --short`: `M docs/design.md`, `M talaria/tools.py`, `M tests/test_write_tools.py` (6-line stub-test deletion only), untracked `tests/test_wait.py`.
- `rg 'time\.sleep|sleep\(' tests/test_wait.py talaria/tools.py`: test file has `FakeClock.sleep` (advances `self.now`, no wall sleep), a boom patch of `talaria.tools.time.sleep`, and a test name; production sleeps only via injected `sleep` or `time.sleep` after a remaining>0 check.
- Independent replay (MockTransport, `FakeClock`, `extra=True` + `parent_agent`): running→completed polls `/api/orchestration/shell` twice, never `/threads/`; approval on first poll sleeps `[]`; timeout=10/interval=5 → sleeps `[5, 5]`, 3 shell GETs; omitted timeout/interval coerce to 300 / 5; `interval=1` floors to 2. Wall time for a 12s fake wait: 0.0006s, sleeps `[5, 5, 2]`.
- 401 body `{"token": "tok-test-LEAK-ME", "Authorization": "Bearer …"}` → `{"error": "not authenticated", "hint": "run \`hermes t3code login <pairing-url>\` …"}` with no token. Synthetic HTTP 500 `upstream token=tok-test-LEAK-ME` appears in `T3ApiError.hint` — WI-2 body-excerpt contract, not the wait handler stuffing the bearer. Junk `args` (`None` / `str` / `int` / non-string `thread_id`) → JSON, no raise. Missing ctx → stub JSON.
- Socket-deny (`socket.socket` / `create_connection`): `import talaria` + `register(FakeCtx)` succeeds; 8 tools including `t3_wait`.
- Checked against t3code `OrchestrationLatestTurn` / `OrchestrationThreadShell` / `OrchestrationThreadDetailSnapshot` / `EnvironmentOrchestrationHttpApi.shellSnapshot` (`orchestration.ts`, `environmentHttp.ts`) and server `mapLatestTurn` (unknown DB state → `"running"`). Shell snapshot is the full `threads[]` array (no page). Pending flags are shell-only. `T3_WAIT_SCHEMA` already documents default 5s / floor 2s / timeout 300s.
