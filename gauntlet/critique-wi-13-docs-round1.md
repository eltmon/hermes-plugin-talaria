# WI-13 docs — round 1
Verdict: NOT_YET

## Criteria
- CORRECTNESS: Not met. AC1 holds: every `plugin.yaml` `config_schema` key and every `T3CODE_*` secret name in `talaria/` appears in [`README.md`](/home/eltmon/Projects/hermes-plugin-talaria/README.md). Schema keys `t3connect` (`enabled`, `relay_url`, `hosted_app_url`, `clerk_publishable_key`, `clerk_oauth_client_id`), `environments` (`{ base_url }`), `default_environment`, `allow_gateway_injection`; secrets `T3CODE_TOKEN_<NAME>` (mangling `my-laptop` → `T3CODE_TOKEN_MY_LAPTOP`), `T3CODE_CLERK_ACCESS_TOKEN`, `T3CODE_CLERK_REFRESH_TOKEN`, `T3CODE_CLERK_PKCE_PENDING`, `T3CODE_DPOP_KEY`. `get_config`/`set_config` call sites match that set. The install → enable → login → `t3_prompt` walkthrough has no extra config/env/secret-export step; `eltmon/hermes-plugin-talaria` is a public owner/repo identifier (does not need the index); login does not import `cryptography` (lazy in [`talaria/dpop.py`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/dpop.py)). Pairing and connect ceremonies are verbatim (`hermes t3code login <pairing-url>`, `hermes t3code connect`, `hermes t3code connect --code '…'`). Tested t3code **0.0.37**. `allow_gateway_injection` names the settings key Hermes `get_config` actually reads and the host grant. That is not enough for AC2 / CP-4. [`docs/design.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/design.md) CP-4 claims the 2026-08-31 404 on `NousResearch/hermes-plugin-index` is closed. Independent probe: `curl` of `https://raw.githubusercontent.com/NousResearch/hermes-plugin-index/main/index.json` is HTTP 404; `gh repo view NousResearch/hermes-plugin-index` is `Could not resolve to a Repository`. Hermes `DEFAULT_INDEX_URL` still points at that 404. A live community index exists at [Revell-ai/hermes-plugin-index](https://github.com/Revell-ai/hermes-plugin-index) (`index.json` serving entries; PRs add to that file; clients opt in via `plugins.index_url`). PRD fallback (seed schema + Nous Discord `#plugins-skills-and-skins`) is not recorded as the live path. The draft entry itself is fine (see CONTRACT). Do not treat “no index PR” as a defect — PRD says do not submit until M1+M2 green.
- CONTRACT: Met for the docs surfaces. [`README.md`](/home/eltmon/Projects/hermes-plugin-talaria/README.md) has install/enable, both auth walkthroughs, config table, injection note, tested version. [`CONTRIBUTING.md`](/home/eltmon/Projects/hermes-plugin-talaria/CONTRIBUTING.md) has venv setup, `python -m pytest tests/ -q`, `hermes plugins doctor . --ci`. [`docs/PRD.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/PRD.md) status flipped to `M1+M2 implemented · 2026-09-01`. [`docs/design.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/design.md) already had the WI-10 CP-3 frame schema; CP-4 section added. [`docs/plugin-index-entry.json`](/home/eltmon/Projects/hermes-plugin-talaria/docs/plugin-index-entry.json) matches `PluginIndexEntry` / seed `hermes_cli/data/plugin_index.json` (`name`, `repo` `owner/name`, `ref`, optional `homepage`/`tags`/`capabilities`/`api_version`/`added_at`; no extra fields). `ref` is the documented placeholder `REPLACE_WITH_HEAD_SHA` (critic allows that). `name: t3code` matches [`plugin.yaml`](/home/eltmon/Projects/hermes-plugin-talaria/plugin.yaml).
- SECURITY: Met. README tells operators tokens never go in `config.yaml` / `plugin-data/` / the install dir; secret table names the hermes `~/.hermes/.env` surface. Clerk pk/client id are labeled public identifiers. No new secret names in code that the README omits. NFR-2 documentation holds for this item.
- TESTS: Met as a docs item’s mechanical check. `python3 -m pytest tests/ -q` → 242 passed in 0.37s. Config/secret inventory was a code-vs-README grep, not a copy of the README. CP-4 was probed against the live GitHub/raw URLs and `gh`, not against the design.md sentence. No `time.sleep` added.
- SIMPLICITY: Met. files_scope is README / CONTRIBUTING / `docs/PRD.md` / `docs/design.md`, plus the two draft index files CP-4 needs. No extra onboarding framework.

## Defects
1. **CP-4 recorded the opposite of the live index location.** [`docs/design.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/design.md) CP-4, [`docs/plugin-index-entry.md`](/home/eltmon/Projects/hermes-plugin-talaria/docs/plugin-index-entry.md), and README “Community index” all say canonical is [NousResearch/hermes-plugin-index](https://github.com/NousResearch/hermes-plugin-index) and “the 2026-08-31 404 is closed.” Independent probe 2026-09-01: that repo does not exist (HTTP 404 on the GitHub page and on `…/main/index.json`; `gh` GraphQL cannot resolve the name). Hermes [`hermes_cli/plugin_index.py`](/home/eltmon/Projects/hermes-agent/hermes_cli/plugin_index.py) `DEFAULT_INDEX_URL` still uses that URL, so a stock client falls back to the bundled seed. The live index you can actually PR is [Revell-ai/hermes-plugin-index](https://github.com/Revell-ai/hermes-plugin-index) (public; `https://raw.githubusercontent.com/Revell-ai/hermes-plugin-index/main/index.json` returns entries; contributing is “open a PR that adds an entry to `index.json`”; clients set `plugins.index_url` to that raw URL). PRD CP-4 fallback — seed `hermes_cli/data/plugin_index.json` as schema, promote via Discord `#plugins-skills-and-skins` — is not mentioned outside the PRD itself. Keep the drafted JSON (placeholder SHA is fine). Rewrite CP-4 to the probe: Nous URL is still what Hermes defaults to and still 404s; Revell-ai is the live PR target until Nous publishes; Discord remains the promote path; do not submit from this item. A reader following the current “open a PR against NousResearch/hermes-plugin-index” step hits a 404.

## Probes run
- `python3 -m pytest tests/ -q` → 242 passed in 0.37s. `git status --short`: `M README.md`, `M docs/PRD.md`, `M docs/design.md`, untracked `CONTRIBUTING.md`, `docs/plugin-index-entry.json`, `docs/plugin-index-entry.md`. `git diff --stat`: README +256, PRD status one-line flip, design +12 (CP-4 section).
- Config/secret inventory (`plugin.yaml` `config_schema` + `get_config`/`set_config` + `T3CODE_*` in `talaria/` vs README): all schema keys and all five secret names present. Nested env parser also reads `environment_id` / `environmentId` / `mode`; README documents `{ base_url }` and lookup by `environmentId` / mode A vs B — not a missing secret/config-schema key.
- Walkthrough vs Hermes installer: `hermes plugins install <owner/repo>` and `enable` exist; GitHub `eltmon/hermes-plugin-talaria` is public (no index needed). [`talaria/__init__.py`](/home/eltmon/Projects/hermes-plugin-talaria/talaria/__init__.py) `register()` does not import `cryptography` (dpop lazy-imports on first T3 Connect DPoP use). Missing `python_dependencies` is a warning, not a load hard-fail. Login path needs no `config.yaml` edit; `login()` upserts `environments.<name> = {base_url}` and `set_secret(T3CODE_TOKEN_…)`. `t3_prompt` required args are `thread_id` + `text` (walkthrough names both).
- CP-4 live: `curl` `https://raw.githubusercontent.com/NousResearch/hermes-plugin-index/main/index.json` → HTTP 404. `gh repo view NousResearch/hermes-plugin-index` → `Could not resolve to a Repository`. `curl` `https://raw.githubusercontent.com/Revell-ai/hermes-plugin-index/main/index.json` → 200, JSON `plugins` list. Draft entry parses; required `name`/`repo`/`ref` present; `repo` has one slash; no fields outside `PluginIndexEntry`.
- Residuals, not blockers: walkthrough says `t3 pair` (PRD wording) not t3code’s `npx t3 pair`; “start the server” has no `npx t3 serve`; a loopback pairing URL is not reachable from another host (`npx t3 pair --tailscale` is the t3code doc for that); empty-thread `t3_new_thread` needs `project_id`/`title`/`model_selection` not spelled in the shortest path; session start command is “Start a Hermes session” not `hermes`. design.md M3 checklist still unchecked. PATH `hermes` on this machine is v0.9.0 and has no `plugins doctor` — that is a stale install, not the hermes-agent checkout CONTRIBUTING targets.
